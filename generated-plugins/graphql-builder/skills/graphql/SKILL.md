---
name: graphql
description: Generate GraphQL schemas and resolvers
allowed-tools:
  - Read
  - Write
  - Edit
  - Bash
  - Grep
  - Glob
---

# graphql-builder

Generate GraphQL schemas and resolvers.

## Tools Available

- **Read** — Read files from the filesystem
- **Write** — Write files to the filesystem
- **Edit** — Make targeted edits to existing files
- **Bash** — Execute shell commands
- **Grep** — Search file contents with regex
- **Glob** — Find files by pattern matching

## Usage

Invoke the `graphql` skill to Generate GraphQL schemas and resolvers. The skill will analyze the relevant codebase context and generate appropriate output.
