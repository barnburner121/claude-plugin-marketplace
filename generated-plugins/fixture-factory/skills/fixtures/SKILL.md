---
name: fixtures
description: Generate test fixtures for data models
allowed-tools:
  - Read
  - Write
  - Edit
  - Bash
  - Grep
  - Glob
---

# fixture-factory

Generate test fixtures for data models.

## Tools Available

- **Read** — Read files from the filesystem
- **Write** — Write files to the filesystem
- **Edit** — Make targeted edits to existing files
- **Bash** — Execute shell commands
- **Grep** — Search file contents with regex
- **Glob** — Find files by pattern matching

## Usage

Invoke the `fixtures` skill to Generate test fixtures for data models. The skill will analyze the relevant codebase context and generate appropriate output.
