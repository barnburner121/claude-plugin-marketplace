---
name: timeseries
description: Set up time series data storage
allowed-tools:
  - Read
  - Write
  - Edit
  - Bash
  - Grep
  - Glob
---

# timeseries-setup

Set up time series data storage.

## Tools Available

- **Read** — Read files from the project
- **Write** — Write new files to disk
- **Edit** — Edit existing files in place
- **Bash** — Run shell commands
- **Grep** — Search file contents with regex
- **Glob** — Find files by pattern

## Usage

Invoke the `timeseries` skill to Set up time series data storage. The skill analyzes your project structure and generates the appropriate code and configuration files.
