---
name: rate-limit
description: Generate rate limiting middleware
allowed-tools:
  - Read
  - Write
  - Edit
  - Bash
  - Grep
  - Glob
---

# rate-limiter

Generate rate limiting middleware.

## Tools Available

- **Read** — Read files from the filesystem
- **Write** — Write files to the filesystem
- **Edit** — Make targeted edits to existing files
- **Bash** — Execute shell commands
- **Grep** — Search file contents with regex
- **Glob** — Find files by pattern matching

## Usage

Invoke the `rate-limit` skill to Generate rate limiting middleware. The skill will analyze the relevant codebase context and generate appropriate output.
