---
name: test-parallel
description: Configure test parallelization
allowed-tools:
  - Read
  - Write
  - Edit
  - Bash
  - Grep
  - Glob
---

# test-parallelizer

Configure test parallelization.

## Tools Available

- **Read** — Read files from the filesystem
- **Write** — Write files to the filesystem
- **Edit** — Make targeted edits to existing files
- **Bash** — Execute shell commands
- **Grep** — Search file contents with regex
- **Glob** — Find files by pattern matching

## Usage

Invoke the `test-parallel` skill to Configure test parallelization. The skill will analyze the relevant codebase context and generate appropriate output.
