---
name: paginate
description: Generate pagination implementations
allowed-tools:
  - Read
  - Write
  - Edit
  - Bash
  - Grep
  - Glob
---

# pagination-gen

Generate pagination implementations.

## Tools Available

- **Read** — Read files from the filesystem
- **Write** — Write files to the filesystem
- **Edit** — Make targeted edits to existing files
- **Bash** — Execute shell commands
- **Grep** — Search file contents with regex
- **Glob** — Find files by pattern matching

## Usage

Invoke the `paginate` skill to Generate pagination implementations. The skill will analyze the relevant codebase context and generate appropriate output.
