---
name: pwa
description: Generate PWA setup: service worker, manifest
allowed-tools:
  - Read
  - Write
  - Edit
  - Bash
  - Grep
  - Glob
---

# pwa-builder

Generate PWA setup: service worker, manifest.

## Tools Available

- **Read** — Read files from the filesystem
- **Write** — Write files to the filesystem
- **Edit** — Make targeted edits to existing files
- **Bash** — Execute shell commands
- **Grep** — Search file contents with regex
- **Glob** — Find files by pattern matching

## Usage

Invoke the `pwa` skill to Generate PWA setup: service worker, manifest. The skill will analyze the relevant codebase context and generate appropriate output.
