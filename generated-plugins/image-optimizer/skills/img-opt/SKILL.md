---
name: img-opt
description: Generate image optimization pipelines
allowed-tools:
  - Read
  - Write
  - Edit
  - Bash
  - Grep
  - Glob
---

# image-optimizer

Generate image optimization pipelines.

## Tools Available

- **Read** — Read files from the filesystem
- **Write** — Write files to the filesystem
- **Edit** — Make targeted edits to existing files
- **Bash** — Execute shell commands
- **Grep** — Search file contents with regex
- **Glob** — Find files by pattern matching

## Usage

Invoke the `img-opt` skill to Generate image optimization pipelines. The skill will analyze the relevant codebase context and generate appropriate output.
