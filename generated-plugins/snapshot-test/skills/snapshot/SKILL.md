---
name: snapshot
description: Generate snapshot tests for UI components
allowed-tools:
  - Read
  - Write
  - Edit
  - Bash
  - Grep
  - Glob
---

# snapshot-test

Generate snapshot tests for UI components.

## Tools Available

- **Read** — Read files from the filesystem
- **Write** — Write files to the filesystem
- **Edit** — Make targeted edits to existing files
- **Bash** — Execute shell commands
- **Grep** — Search file contents with regex
- **Glob** — Find files by pattern matching

## Usage

Invoke the `snapshot` skill to Generate snapshot tests for UI components. The skill will analyze the relevant codebase context and generate appropriate output.
