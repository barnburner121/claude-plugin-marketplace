---
name: sitemap
description: Generate XML sitemaps from routes
allowed-tools:
  - Read
  - Write
  - Edit
  - Bash
  - Grep
  - Glob
---

# sitemap-gen

Generate XML sitemaps from routes.

## Tools Available

- **Read** — Read files from the filesystem
- **Write** — Write files to the filesystem
- **Edit** — Make targeted edits to existing files
- **Bash** — Execute shell commands
- **Grep** — Search file contents with regex
- **Glob** — Find files by pattern matching

## Usage

Invoke the `sitemap` skill to Generate XML sitemaps from routes. The skill will analyze the relevant codebase context and generate appropriate output.
