---
name: smoke
description: Generate deployment smoke tests
allowed-tools:
  - Read
  - Write
  - Edit
  - Bash
  - Grep
  - Glob
---

# smoke-test

Generate deployment smoke tests.

## Tools Available

- **Read** — Read files from the filesystem
- **Write** — Write files to the filesystem
- **Edit** — Make targeted edits to existing files
- **Bash** — Execute shell commands
- **Grep** — Search file contents with regex
- **Glob** — Find files by pattern matching

## Usage

Invoke the `smoke` skill to Generate deployment smoke tests. The skill will analyze the relevant codebase context and generate appropriate output.
