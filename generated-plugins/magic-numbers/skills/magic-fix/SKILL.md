---
name: magic-fix
description: Find magic numbers/strings and extract to constants
allowed-tools: Read, Write, Edit, Bash, Grep, Glob
---

# magic-numbers

Find magic numbers/strings and extract to constants

## Tools

This skill uses the following tools:

- **Read** — Read files from the filesystem to analyze content
- **Write** — Write new files or overwrite existing files
- **Edit** — Make targeted edits to existing files
- **Bash** — Execute shell commands for scanning, building, and automation
- **Grep** — Search file contents with regex patterns
- **Glob** — Find files by name patterns

## Usage

Invoke this skill by asking Claude to Find magic numbers/strings and extract to constants.
