---
name: a11y-test
description: Generate accessibility tests (WCAG)
allowed-tools:
  - Read
  - Write
  - Edit
  - Bash
  - Grep
  - Glob
---

# accessibility-test

Generate accessibility tests (WCAG).

## Tools Available

- **Read** — Read files from the filesystem
- **Write** — Write files to the filesystem
- **Edit** — Make targeted edits to existing files
- **Bash** — Execute shell commands
- **Grep** — Search file contents with regex
- **Glob** — Find files by pattern matching

## Usage

Invoke the `a11y-test` skill to Generate accessibility tests (WCAG). The skill will analyze the relevant codebase context and generate appropriate output.
